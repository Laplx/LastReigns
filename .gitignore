# secrets
.env.local
.env

# node
node_modules/
npm-debug.log*

# claude temp / local
.tmp_*
*.local.log
.serena/

# dev-only preview / scratch artifacts (regenerated by scripts; never published)
portrait-preview.*
portrait-compare.*
_*.html
op-*.svg
pv-*.png

# os
.DS_Store
Thumbs.db

# saves exported during play (optional, keep out of repo)
/saves/
