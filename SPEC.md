# 《最后的统治者》— 设计与实现规格 (SPEC)

> 基于《独裁者手册》的讽刺向政治模拟。背景：中非虚构小国 **恩加拉共和国**。
> 玩家刚通过政变上台，目标是统治得久、捞得多、死在床上——分数越高越好。
> **核心讽刺**：不仅是该书"维系最小关键联盟"的冷酷逻辑，更是两难与平衡的政治艺术。

---

## 0. 顶层原则（贯穿所有模块）

1. **核心数字不直报**。六个核心指标的内部值不以数字文本展示；当前 UI 使用无刻度比例条、氛围词、提示文本和结果摘要表达变化。国土、私产、结局积分等经营/结算信息可按当前界面直接显示。
2. **积分只在结局揭晓**，中途绝不显示。
3. **尽量没有"明显正确解"**。多数事件是双输/权衡，不是送分。
4. **效果不提前揭示**，选完才看到叙事化结果与变化摘要（选项文字可暗示方向，但不提前列出具体效果）。
5. **LLM 永不直接写状态**：它只输出受约束的结构化意图，由规则引擎校验+钳制后落账。LLM 是增强层，不是依赖层——断网也能完整玩通（预设兜底）。
6. **氛围词不死板**：每档一个短语池 + 防重复；联网时由 LLM 结合真实状态即兴生成，静态池退化为兜底。

---

## 1. 指标系统

### 1.1 六个核心指标（内部 0–100，界面不直报数值）
| key | 名称 | 图标 | 初值 | 方向 | 失败后果 |
|---|---|---|---|---|---|
| `army` | 军队 | 🪖 | 50 | **中段安全** | 过低→政变；过高→军方坐大/架空 |
| `elite` | 精英 | 🤝 | 50 | **中段安全** | 过低→暗杀/崩盘；过高→寡头傀儡 |
| `morale` | 民心 | 🌾 | 50 | **中段安全** | 过低→起义；过高→狂热失控 |
| `intl` | 国际 | 🌍 | 50 | **中段安全** | 过低→孤立/制裁/失认；过高→外部渗透/干预 |
| `finance` | 财政 | 🏦 | 55 | 高为佳 | 过低→欠饷哗变、忠诚折价 |
| `health` | 领袖健康 | 💊 | 100 | 高为佳 | 归零→自然死亡（最高荣誉结局） |

危险阈值与概率事件见 `engine.js`：
- 当前实现为“单次警讯”：若首次发现时已到极端区，直接显示最后警讯；否则显示普通警讯。同一危机方向展示一次后不再补第二张；进入极端阈值时，年末才滚动终局概率。同一年最多插入一张危机警讯卡，多指标同时危险时合并展示。
- `intl <= 20` 会进入制裁/失认状态，财富增速受损；`intl` 过高则代表外部干预、观察团、基金会和反对派议程坐大。

### 1.2 隐藏指标（不作为主面板数值显示，主要驱动逻辑/结局）
- 各关键人物个人忠诚度（见 §2）
- 邻国紧张度 `borderTension`（影响边境事件触发率）
- 王朝合法性 `legacy`（影响结局系数）
- **民心氛围 / 历史叙事** 二分（`morale` 驱动短期可见风险，`historyNarrative` 只进结局倍率）

### 1.3 装饰指标（当前 4 个，**不进结算**）
当前实现：`health_care` 医疗、`education` 教育、`capital` 首都形象工程、`press` 媒体自由指数。
作用：制造干扰信息、承载讽刺、填充通知卡。可微弱反向喂事件触发权重，绝不进积分。界面以粗标签呈现。

---

## 2. 关键人物系统
- 当前人物库为 **10 种角色类型**，每种角色带一组随机姓名；每局随机抽 **4 人** 常驻。
- 每人：`id, name, canonicalName, aliases, title, traits[], hiddenInterest, loyalty, competence, allies, rivals, memory[]`。
- 忠诚在状态区以信号条/信号色表达，人物详情以标签文案表达；能力以粗标签表达。
- 人物关系 `allies/rivals` 会影响忠诚涟漪，并在事件结果或人物详情中形成叙事反馈。
- 任意 **2 人同时叛变** → 触发“精英崩溃”终局。单人叛变会削弱精英，军方人物叛变还会影响军队。

---

## 3. 年度流程
```
① 财政年度预算（每 3 年一次：军队 / 精英福利 / 民生 / 个人账户，无正确比例）
② 每年 3–5 张卡（普通卡 / 特殊卡(LLM) / 通知卡 / 事件链 / 危机警告）
③ 若当年尚未私下接触人物，年末进入“年终召见”，从 4 位重臣中选 1 人执行一次动作
④ 年末隐藏推进：健康衰减、局势漂移、事件链激活、危机判定
```
**不作为的代价**：事件链连续拖延会自动向更坏节点推进，并提示「有些事情已经无法回头」。同一链节点再次出现时会以续报形式呈现，避免原样复读。

---

## 4. 事件卡系统
- 类型：`normal`（预设骨架，当前 120 张）/ `notify`（通知卡，当前 30 张，零核心变动）/ `special`（LLM 生成叙事）/ `chain`（事件链节点）。
- 分层：`early` 1–7 年、`mid` 4–24 年、`late` 15 年后；危机或健康恶化会提前开放 `late`，并保留低概率越期抽取，避免卡池机械切断。
- 每张预设事件必须有 `theme` 与 `stages`。`themeCooldowns` 会让相近主题在数年内降权，避免 NGO、观察团、矿区、宫廷举报、雕像工程等重复刷屏；无可用卡时会降级放宽。
- 卡结构见 `data/events.json`。每个 option 携带**枚举档位**效果（如 `army: +mid`），引擎映射成数值并钳制；不写数字。
- 事件展示会把事件文本中的人物 canonical 名称替换成本局随机姓名；事件可通过 `speaker` 指向说话人，并通过 `speakerRole` / `portraitId` 指定普通职位或明确画像。无显式画像时按关键人物、主题、关键词兜底匹配。
- 人物画像定义见 `data/portraits.json`：10 个关键人物角色 + 12 类普通职位。美术资源打包在 `data/portraits-art.json`（运行时一次性加载、离线可用）：`peeps` 为 Open Peeps 半身像（CC0，经 DiceBear 按职业配好部件并染成墨金配色），`icons` 为 Lucide 职业图标（ISC）。`src/portraits.js` 据此合成：事件卡右侧为人像水印（不叠 icon），年终召见/人物详情为人像 + 职业 icon 圆徽，状态栏人物栏只显示职业 icon。美术缺失时回退到内置自绘墨金半身像。
- 人物关系：`allies/rivals` 不只产生忠诚涟漪，事件结果会根据直接影响的人物追加盟友/宿敌反应，使关系网进入叙事层。
- **事与愿违（三级，有迹可循）**：
  - 一级 信息失真（高忠诚也可能）：执行忠实但情报错→结果偏差。
  - 二级 私利夹带（中忠诚）：主效果达成 + 一个不利副效果，叙事轻描淡写暗示。
  - 三级 主动背刺（危险忠诚，且**已出现 ≥2 条前兆**才可触发）：把命令/意图透露给对立面，叙事模糊埋线索。
  - 规则引擎强制：背刺前置计数器；LLM 输出 `fidelity≠faithful` 时若缺少 `foreshadow_clue`，引擎补一条默认线索，不重采以节约等待时间。

---

## 5. 事件链系统（副本机制）
- 当前内置 **20 条**事件链，每条 **4–8 个节点**；同时最多激活 **2 条**链，独立进度，可相互干扰。
- 链激活后先展示公告，随后链节点会在年度卡流中优先推进；每条链每年最多推进一次，全局每年最多推进 **2 条链节点**，不再作为普通事件随机抽取。
- 若第 4 年仍无事件链，`forceSeed` 链会兜底激活；活跃链按逾期程度排序，避免只激活但长期不推进。
- 链节点标题必须带链名，避免玩家失去连续感；首节点原则上不直接结链，链触发后应基本走完主要流程。
- 内置主题覆盖：继承、矿区、边境、民主压力、宫廷内斗、税籍、债务、国家队、媒体排名、雕像、医院、教育、安全局档案、油田、地方诸侯、教会、流亡者、健康公告、国际法庭、货币危机。
- 定义见 `data/chains.json`（触发条件、节点序列、分支、结局系数影响）。

---

## 6. LLM 集成（三场景，其余预设/规则兜底）
后端代理 `POST /api/llm`（DeepSeek，OpenAI 兼容，key 在 `.env.local`）。
1. **特殊事件叙事**：后台传入当前状态摘要与近期冷却主题 → 生成 ≤200 字叙事 + 2–3 选项，并给出 `theme` 与 `stages`（选项效果由引擎档位计算，LLM 只包装）。生成完成后进入优先队列，在预算/危机/事件链之后、普通事件之前使用，不中途 loading。严格 JSON schema。
2. **顾问私谈 / 动作选项**：玩家每年可私下接触一名人物。当前启用的是“年终召见”卡：玩家在 4 位重臣中选 1 人，联网时 LLM 以该人物 persona 生成 3 个动作选项；离线或联网失败时使用规则模板兜底：
   ```jsonc
   {
     "fidelity": "faithful|discounted|feigned|betrayal",
     "public_narrative": "当面回应(玩家可见)",
     "foreshadow_clue": "fidelity≠faithful 时必填，埋线索不点破",
     "proposed_effects": { "...": "+small|-mid|..." },
     "hidden_effects": { "...": "+small|-mid|..." },   // 玩家不可见(私利/背刺真实后果)
     "memory_note": "供日后叙事引用"
   }
   ```
   引擎校验 fidelity 与忠诚区间一致，钳制档位，落账。每年限 1 次。代码中保留过往直接从人物面板进入自然语言/深谈的 `engagePerson` 路径，但当前版本暂不启用。
3. **结局词条**：传完整记录 → 生成 ~500 字"维基式"身后评价 + 一句"名言" + 命中成就标签。生前可花财富设"传记委员会"→ 额外生成"官方认可版"，与"民间流传版"并列。
- **离线兜底**：LLM 不可用时用预设事件、静态氛围池、规则版顾问动作和预设结局词条，游戏完整可玩。
- **链节点改写**：LLM 只允许改写当前节点 `narrative`，不得改变标题、选项、效果、`goto`、`escalateTo` 或新增会改变判断的信息。

---

## 7. 积分结算（仅结局显示）
```
基础分   = 在位年数 × 领土系数 × 10
财富分   = 个人账户(亿$) × 80   (海外账户×1.5；被制裁时国内资产可能冻结)
王朝分   = 继承人成功执政 +500 / 内战 -200
结局系数 = 自然死亡(平稳交接)×2.5 / 自然死亡(身后内战)×1.8 / 流亡×0.5 / 被捕×0.2 / 政变身亡×0
隐性成就 = LLM 词条命中标签，各 +50~+200（玩家事先不知存在）
```
成就示例："货币以您命名" / "执政超过15年" / "从未举行过选举" / "国际特赦组织专题报告主角" / "子孙三代执政"。

---

## 8. 其他确认的机制
- **年报有，季报无**。
- 单界面 + 浮层；不做 GDP 等经济独立面板（叙事性丰富而非数字性丰富）。
- **档案室**：可翻阅历史事件（标题+结果，无数值），供复盘线索 & LLM 引用历史。
- **健康叙事化衰减**：基础每年 -4~-7，决策可加速/减缓；衰减率不可见，年报氛围暗示。
- **一次性资源**：国际信誉额度、家族遗产（用完即止）。
- **历史解释权**：生前决策版（晚年设传记委员会，花财富买官方叙事）。

---

## 9. 技术栈与结构
- 纯前端 ES modules + JSON 数据；零构建。**Node 零依赖 dev server**（`server.js`）静态托管 + `/api/llm` 代理。
- 运行：`npm start` → http://localhost:5173 。
```
index.html  styles.css  server.js  package.json  .env.local(gitignored)
/src
  game.js         入口/编排：启动、年度循环、阶段调度
  engine.js       状态机：指标钳制、预算、推进、危机、积分
  atmosphere.js   区间→氛围词(池+防重复)，可被 LLM 覆盖
  events.js       抽卡、效果落账、事与愿违判定
  chains.js       事件链推进
  llm.js          provider 适配、schema 校验、三场景、兜底
  ui.js           渲染：面板/人物/卡片/年报/浮层/结局
  state.js        全局状态、存档导出、RNG(可注入种子)
  portraits.js    画像注册表/解析、合成渲染（人像+icon/水印/迷你icon）、自绘兜底
/data
  events.json  people.json  portraits.json  portraits-art.json  chains.json  atmosphere.json  decorative.json
  portraits-op/  icons/   画像源文件（Open Peeps SVG / Lucide icon 片段，打包进 portraits-art.json）
/scripts
  validate-data.mjs  数据校验：数量、主题、阶段、链深度、跳转可达性、notify 零核心变动、画像与美术资源完整
（画像构建脚本：scripts/fetch-peeps.mjs 拉取半身像 / scripts/fetch-icons.mjs 拉取职业 icon / scripts/pack-art.mjs 打包为 portraits-art.json）
```

## 10. 实现顺序（里程碑）
P0 骨架 → P1 引擎 → P2 事件卡 → **P3 UI（纯规则版完整可玩）** → P4 链 → P5 LLM → P6 结局 → P7 打磨。
