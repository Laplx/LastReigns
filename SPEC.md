# 《最后的统治者》— 设计与实现规格 (SPEC)

> 基于《独裁者手册》的讽刺向政治模拟。背景：中非虚构小国 **恩加拉共和国**。
> 玩家刚通过政变上台，目标是统治得久、捞得多、死在床上——分数越高越好。
> **核心讽刺**：不仅是该书"维系最小关键联盟"的冷酷逻辑，更是两难与平衡的政治艺术。

---

## 0. 顶层原则（贯穿所有模块）

1. **数字全隐藏**。五条核心指标只以"氛围词"呈现，不显示数值，也不显示比例条（条本身会被脑补成数字）。
2. **积分只在结局揭晓**，中途绝不显示。
3. **尽量没有"明显正确解"**。多数事件是双输/权衡，不是送分。
4. **效果不提前揭示**，选完才看到叙事化结果（选项文字可暗示方向，但无数字/箭头）。
5. **LLM 永不直接写状态**：它只输出受约束的结构化意图，由规则引擎校验+钳制后落账。LLM 是增强层，不是依赖层——断网也能完整玩通（预设兜底）。
6. **氛围词不死板**：每档一个短语池 + 防重复；联网时由 LLM 结合真实状态即兴生成，静态池退化为兜底。

---

## 1. 指标系统

### 1.1 五条核心指标（隐藏数值，0–100，内部存储）
| key | 名称 | 图标 | 初值 | 方向 | 失败后果 |
|---|---|---|---|---|---|
| `army` | 军队忠诚 | 🪖 | 65 | 高为佳 | 归零→政变身亡 |
| `elite` | 精英联盟 | 🤝 | 60 | 高为佳 | 归零→暗杀/叛逃 |
| `intl` | 国际压力 | 🌍 | 30 | **低为佳** | 过高→制裁、援助断、财富增速腰斩 |
| `unrest` | 民众张力 | 🌾 | 35 | **低为佳** | 过高→起义（不直接致命，可镇压） |
| `health` | 领袖健康 | 💊 | 100 | 高为佳 | 归零→自然死亡（最高荣誉结局） |

危险阈值与概率事件见 `engine.js`：
- army<20：每年 10% 政变；elite<25：每年 15% 暗杀/叛逃；
- intl>临界：制裁、财富增速 -50%；unrest>临界：起义概率上升。

### 1.2 隐藏指标（不显示，仅驱动逻辑/结局）
- 各关键人物个人忠诚度（见 §2）
- 邻国紧张度 `borderTension`（影响边境事件触发率）
- 王朝合法性 `legacy`（影响结局系数）
- **街头情绪 / 历史叙事** 二分（unrest 拆短期可见 vs 长期 `historyNarrative`，后者只进结局）

### 1.3 装饰指标（4→6 个，**不进结算**，封顶 6）
诱饵型（该被理性忽视的公共物品）：`economy` 经济增长、`health_care` 医疗、`education` 教育。
虚荣型（面子/合法性剧场）：`capital` 首都形象工程、`football` 国家足球队、`press` 媒体自由指数。
作用：制造干扰信息、承载讽刺、填充通知卡。可微弱反向喂事件触发权重，绝不进积分。3 档粗标签，无数字。

---

## 2. 关键人物系统
- 人物池 8–10 人，每局随机抽 **4 人** 常驻。
- 每人：`id, name, title, traits[], hiddenInterest, loyalty(0–100,隐藏)`。
- 忠诚只显示**信号色**（loyal/ok/uneasy/danger），不显示数值。
- 任意 **2 人同时叛变** → 触发"精英崩溃"事件链。单人叛变可经事件处理，处理方式影响他人态度。

---

## 3. 年度流程（每年约 60–90 秒）
```
① 预算分配（军队 / 精英福利 / 民生 / 个人账户，无正确比例）
② 事件卡 1–3 张（普通卡 / 特殊卡(LLM) / 通知卡）
③ 年末述职（顾问一句话氛围描述，无数字）
④ 隐藏推进：健康叙事化衰减、事件链推进、概率危机判定
```
**不作为的代价**：某事件连续两年被回避/拖延 → 自动向最坏推进一步 + 通知卡「有些事情已经无法回头」。

---

## 4. 事件卡系统
- 类型：`normal`（预设骨架，~30 张）/ `notify`（通知卡，~20–25%，零核心变动）/ `special`（LLM 生成叙事）/ `chain`（事件链节点）。
- 卡结构见 `data/events.json`。每个 option 携带**枚举档位**效果（如 `army: +mid`），引擎映射成数值并钳制；不写数字。
- **事与愿违（三级，有迹可循）**：
  - 一级 信息失真（高忠诚也可能）：执行忠实但情报错→结果偏差。
  - 二级 私利夹带（中忠诚）：主效果达成 + 一个不利副效果，叙事轻描淡写暗示。
  - 三级 主动背刺（危险忠诚，且**已出现 ≥2 条前兆**才可触发）：把命令/意图透露给对立面，叙事模糊埋线索。
  - 规则引擎强制：背刺前置计数器；LLM 输出 `fidelity≠faithful` 时 `foreshadow_clue` 必须非空，否则拒收重采。

---

## 5. 事件链系统（副本机制）
- 同时最多激活 **2 条**链，每条 3–5 年跨度，独立进度，可相互干扰。
- 内置示例链：A 继承人问题 / B 北方矿区叛乱 / C 邻国边境危机 / D 西方民主压力 / E 宫廷内斗。
- 定义见 `data/chains.json`（触发条件、节点序列、分支、结局系数影响）。

---

## 6. LLM 集成（三场景，其余预设）
后端代理 `POST /api/llm`（DeepSeek，OpenAI 兼容，key 在 `.env.local`）。
1. **特殊事件叙事**：传入当前状态摘要 → 生成 ≤200 字叙事 + 2–3 选项（选项效果由引擎档位计算，LLM 只包装）。严格 JSON schema。
2. **顾问私谈 / 自然语言命令**：玩家自然语言下令 → LLM 以该人物 persona 返回：
   ```jsonc
   {
     "fidelity": "faithful|discounted|feigned|betrayal",
     "public_narrative": "当面回应(玩家可见)",
     "foreshadow_clue": "fidelity≠faithful 时必填，埋线索不点破",
     "proposed_effects": [{ "indicator": "...", "dir": "+/-", "band": "small|mid|big" }],
     "hidden_effects": [...],   // 玩家不可见(私利/背刺真实后果)
     "memory_note": "供日后叙事引用"
   }
   ```
   引擎校验 fidelity 与忠诚区间一致，钳制档位，落账。每年限 1 次，消耗少量财富。
3. **结局词条**：传完整记录 → 生成 ~500 字"维基式"身后评价 + 一句"名言" + 命中成就标签。生前可花财富设"传记委员会"→ 额外生成"官方认可版"，与"民间流传版"并列。
- **离线兜底**：LLM 不可用时用预设叙事 + 静态氛围池，并 toast 提示网络问题，游戏完整可玩。

---

## 7. 积分结算（仅结局显示）
```
基础分   = 在位年数 × 领土系数 × 10
财富分   = 个人账户(亿$) × 80   (海外账户×1.5；被制裁时国内资产可能冻结)
王朝分   = 继承人成功执政 +500 / 内战 -200
结局系数 = 自然死亡(平稳交接)×2.5 / 自然死亡(身后内战)×1.8 / 流亡×0.5 / 被捕×0.2 / 政变身亡×0
隐性成就 = LLM 词条命中标签，各 +50~+200（玩家事先不知存在）
```
成就示例："货币以您命名" / "执政超过15年" / "从未举行过选举" / "国际特赦组织专题报告主角" / "子孙三代执政"。

---

## 8. 其他确认的机制
- **年报有，季报无**。
- 单界面 + 浮层；不做 GDP 等经济独立面板（叙事性丰富而非数字性丰富）。
- **档案室**：可翻阅历史事件（标题+结果，无数值），供复盘线索 & LLM 引用历史。
- **健康叙事化衰减**：基础每年 -4~-7，决策可加速/减缓；衰减率不可见，年报氛围暗示。
- **一次性资源**：国际信誉额度、家族遗产（用完即止）。
- **历史解释权**：生前决策版（晚年设传记委员会，花财富买官方叙事）。

---

## 9. 技术栈与结构
- 纯前端 ES modules + JSON 数据；零构建。**Node 零依赖 dev server**（`server.js`）静态托管 + `/api/llm` 代理。
- 运行：`npm start` → http://localhost:5173 。
```
index.html  styles.css  server.js  package.json  .env.local(gitignored)
/src
  game.js         入口/编排：启动、年度循环、阶段调度
  engine.js       状态机：指标钳制、预算、推进、危机、积分
  atmosphere.js   区间→氛围词(池+防重复)，可被 LLM 覆盖
  events.js       抽卡、效果落账、事与愿违判定
  chains.js       事件链推进
  llm.js          provider 适配、schema 校验、三场景、兜底
  ui.js           渲染：面板/人物/卡片/年报/浮层/结局
  state.js        全局状态、存档导出、RNG(可注入种子)
/data
  events.json  people.json  chains.json  atmosphere.json  decorative.json
```

## 10. 实现顺序（里程碑）
P0 骨架 → P1 引擎 → P2 事件卡 → **P3 UI（纯规则版完整可玩）** → P4 链 → P5 LLM → P6 结局 → P7 打磨。
